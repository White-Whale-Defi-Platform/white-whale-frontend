export enum PositionState {
  all = 'all',
  unbonding = 'unbonding',
  active = 'active',
  unbonded = 'unbonded',
}
export enum IncentiveState {
  all = 'all',
  active = 'active',
  upcoming = 'upcoming',
  over = 'over',
}
