export enum PositionState {
  all = 'all',
  unbonding = 'unbonding',
  active = 'active',
  unbonded = 'unbonded',
  withdraw = 'withdraw',
}
export enum IncentiveState {
  all = 'all',
  active = 'active',
  upcoming = 'upcoming',
  over = 'over',
}
