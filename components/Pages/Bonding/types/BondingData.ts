import { ActionType, TokenType } from "../BondingOverview"

export type BondingData = {
    color: string
    actionType: ActionType
    tokenType: TokenType
    value: number
    label: string
  }
  